#!/bin/bash

set -u

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname "$0")" && pwd)"
# shellcheck source=/dev/null
. "${SCRIPT_DIR}/common.sh"

if ensure_runtime_layout; then
    exit 0
fi

log_lifecycle "Failed to refresh runtime layout after upgrade."
exit 1
