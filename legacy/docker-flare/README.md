# Legacy docker-flare Snapshot

这里保存了从历史 `docker-flare` 仓库并入的原始资产，用于参考和留档。

注意：

- 这些内容不代表 `SuperFlare` 当前推荐的部署方式
- 当前主仓库的真实代码与功能演进以根目录源码为准
- 若需要继续抽取旧示例，请优先复制到 `docs/legacy-docker/` 或 `examples/` 中再整理
